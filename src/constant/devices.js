export const devices = {
    mobile: `(max-width: 600px)`,
    // tablet: `(max-width: ${screen.tablet})`,
    // desktop: `(min-width: ${screen.desktop})`,
    // ipadMin: `(max-width: ${screen.ipadMin})`
}